import React from 'react';

const Blogs = () => {
    return (
        <div>
            <div>
                <p>Question: What is a purpose of Context Api?</p>
                <p>Answer:</p>
                <p>In React application data is passed between two components via props. Prop-drilling is a required if data needs to be sent from one component to another.</p>
            </div>
        </div>
    );
};

export default Blogs;